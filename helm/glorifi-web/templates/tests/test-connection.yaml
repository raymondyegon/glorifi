apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "glorifi-web.fullname" . }}-test-connection"
  labels:
    {{- include "glorifi-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "glorifi-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
