apiVersion: v1
kind: Service
metadata:
  name: {{ include "glorifi-web.fullname" . }}
  labels:
    {{- include "glorifi-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "glorifi-web.selectorLabels" . | nindent 4 }}
