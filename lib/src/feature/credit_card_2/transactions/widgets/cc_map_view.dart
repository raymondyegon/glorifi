import 'package:flutter/material.dart';

class CCMapView extends StatelessWidget {
  const CCMapView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 303.0,
      color: Colors.grey,
      child: Center(child: Text("Design Map Here"),),
    );
  }

}